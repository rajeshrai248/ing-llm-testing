# Production Environment Configuration
# Copy this file to .env.production and update values for your deployment

# ===== API Configuration =====

# Backend API host (leave empty to auto-detect from current hostname)
# Examples:
#   - localhost (for same instance)
#   - backend.example.com (for remote backend)
#   - 192.168.1.100 (for specific IP)
#   - (empty to auto-detect)
VITE_API_HOST=localhost

# Backend API port (default: 8000)
VITE_API_PORT=8000

# Backend API protocol (http or https)
VITE_API_PROTOCOL=http

# ===== Application Settings =====

# Enable debug mode (true/false) - do NOT enable in production
DEBUG=false

# Application environment
NODE_ENV=production

# ===== Optional: Monitoring & Analytics =====

# Sentry DSN for error tracking (optional)
# VITE_SENTRY_DSN=

# Google Analytics ID (optional)
# VITE_GA_ID=

# ===== Notes =====
# 1. After changing these values, rebuild the application:
#    npm run build
#
# 2. For secure deployments, use HTTPS:
#    VITE_API_PROTOCOL=https
#
# 3. If the backend is on a different server, update VITE_API_HOST
#    and ensure CORS is configured on the backend
#
# 4. Do NOT commit sensitive data to version control
#    Add .env.production to .gitignore
